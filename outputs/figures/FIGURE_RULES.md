# Quy ước tạo hình

- Tên/caption kiểu `Hình x.y: ...` chỉ dùng trong tài liệu hoặc tên file.
- Không đặt caption `Hình x.y: ...` trực tiếp bên trong nội dung ảnh.
- Trong ảnh chỉ dùng nhãn chức năng ngắn, ví dụ: dữ liệu đầu vào, A*, ma trận chi phí, GA, kết quả.
